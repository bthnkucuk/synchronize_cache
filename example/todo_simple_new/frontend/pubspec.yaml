name: todo_simple_new_frontend
description: Simple TODO app demonstrating offline_first_sync_drift with new API
publish_to: none
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  path_provider: ^2.1.5
  offline_first_sync_drift:
    path: ../../../packages/offline_first_sync_drift
  offline_first_sync_drift_rest:
    path: ../../../packages/offline_first_sync_drift_rest
  json_annotation: ^4.12.0
  uuid: ^4.5.2
  provider: ^6.1.5+1
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  drift_dev: ^2.33.0
  build_runner: ^2.4.15
  json_serializable: ^6.14.0
  mocktail: ^1.0.4
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
