name: example
description: Example app using offline_first_sync_drift.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  offline_first_sync_drift:
    path: ../packages/offline_first_sync_drift
  offline_first_sync_drift_rest:
    path: ../packages/offline_first_sync_drift_rest
  drift: ^2.33.0
  path: ^1.9.1
  json_annotation: ^4.12.0

dev_dependencies:
  drift_dev: ^2.33.0
  build_runner: ^2.15.0
  json_serializable: ^6.14.0
  test: ^1.31.0
